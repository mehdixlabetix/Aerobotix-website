{
  "name": "aerobotix-vitrine",
  "version": "1.0.0",
  "private": true,
  "description": "AeRobotiX INSAT public showcase website",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "check": "node --check app.js && node --check competition.js && node --check join.js && node --check partners.js && node --check api/partnerships.js && node --check build.mjs && node --check server.mjs && node verify.mjs"
  },
  "engines": {
    "node": "22.x"
  }
}
